import { useEffect } from 'react';

/** Stub: MCP chat loading indicator (autosales). */
export function useMcpChatLoadingIndicator() {
  useEffect(() => {
    /* no-op in vado-landing */
  }, []);
  return false;
}
