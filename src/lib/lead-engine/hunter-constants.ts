/** Máximo de empresas que Hunter Discover devuelve por solicitud (API v2). */
export const HUNTER_DISCOVER_MAX_RESULTS = 100;

/** Límite por defecto cuando el usuario activa «Limitar cantidad». */
export const HUNTER_DISCOVER_DEFAULT_LIMIT = 10;
